//- Those variables should be in every page. ALWAYS PASS childID, fullName
- var page = "Nothing"
- var loggedIn = false

doctype html
html
  include ../base/head.pug
  body
    include ../base/navbar.pug
    div(class="container")
      div(class="row")
        a(href="/" class="noLinkTitle backButton")
          span(class="glyphicon glyphicon-menu-left backButton")
      div(class="row")
        h1(class = 'err_msg') Nothing to show here!
